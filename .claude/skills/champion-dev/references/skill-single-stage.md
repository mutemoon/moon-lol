# 单段技能

## 设计与实现

- 适用场景：适用于只包含单个施法动作、一次位移或一次伤害，且没有复杂阶段状态的技能。
- 冷却设置：默认使用 `SkillCooldownMode::AfterCast` 自动冷却模式。
- 流转路径：
  - 在技能注册时使用 `Skill::new` 绑定槽位与对应数值配置。
  - 监听 `EventSkillCast` 并匹配槽位。
  - 直接调用动画、特效、位移或伤害等原子动作函数。

## 典型实现案例

- 盖伦 E 技能：在单个观察者逻辑中，直接派发播放技能动画命令，并伴随触发一个圆形物理伤害区域以及命中音效特效。
